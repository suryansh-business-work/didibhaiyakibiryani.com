# Baked into the Vite/Capacitor production build (`npm run build` → APK via `npx cap sync`).
# Without this, the build falls back to http://localhost:3001 and the APK cannot
# reach the API on a real device — causing "failed to login".
VITE_API_URL=https://server.didibhaiyakibiryani.com/graphql
